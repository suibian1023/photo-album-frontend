# 相册Web应用开发者文档

## 1. 项目概述

本项目是一个基于 Vue 3 的相册 Web 应用，提供用户登录、照片上传、照片分类管理以及照片展示等核心功能。前端采用单页面应用（SPA）架构，使用 Pinia 管理全局状态，Vue Router 实现路由控制。后端 API 可基于 RESTful 风格对接（本阶段提供 API 约定，实际后端可 Mock 或独立实现）。

## 2. 技术栈

| 类别         | 技术选型                                            |
|--------------|-----------------------------------------------------|
| 前端框架     | Vue 3 (Composition API)                            |
| 状态管理     | Pinia                                               |
| 路由         | Vue Router 4                                       |
| UI 组件库    | Element Plus / Naive UI（推荐，可替换，示例文档不依赖具体） |
| HTTP 请求    | Axios                                               |
| 文件上传     | 原生 input + FormData                              |
| 构建工具     | Vite                                                |
| 代码规范     | ESLint + Prettier                                  |

## 3. 目录结构设计

```
src/
├── api/                # API 接口封装
│   ├── auth.js
│   ├── category.js
│   └── photo.js
├── assets/             # 静态资源（图片、样式等）
├── components/         # 公共组件
│   ├── AppLayout.vue   # 主布局（侧边栏 + 内容区）
│   ├── PhotoGrid.vue   # 照片网格展示
│   ├── UploadModal.vue # 上传照片弹窗
│   └── CategorySidebar.vue # 分类侧边栏
├── views/              # 页面视图
│   ├── Login.vue
│   ├── Dashboard.vue   # 主相册页
│   └── CategoryManage.vue # 分类管理页（可选）
├── router/             # 路由配置
│   └── index.js
├── stores/             # Pinia 状态管理
│   ├── userStore.js
│   ├── photoStore.js
│   └── categoryStore.js
├── utils/              # 工具函数
│   ├── request.js      # Axios 实例封装
│   └── auth.js         # token 处理
├── App.vue
└── main.js
```

## 4. 页面结构与路由设计

### 4.1 路由表

| 路径            | 名称            | 组件                | 是否需要登录 | 描述                       |
|-----------------|----------------|---------------------|--------------|----------------------------|
| `/login`        | login          | `Login.vue`         | 否           | 用户登录页                 |
| `/`             | dashboard      | `Dashboard.vue`     | 是           | 相册主页（照片墙+分类侧栏）|
| `/categories`   | categoryManage | `CategoryManage.vue`| 是           | 分类管理（增删改分类）     |
| `/photo/:id`    | photoDetail    | `PhotoDetail.vue`   | 是           | 照片详情页（可选）         |

> 未匹配路由重定向至 `/`

### 4.2 页面布局描述

- **登录页**：用户名/密码表单，提交后调用登录 API，获取 token，保存到 Pinia 和 localStorage，并跳转到 `/`。
- **主相册页（Dashboard）**：
  - 左侧 `CategorySidebar`：展示当前用户所有分类，支持点击切换当前激活分类。
  - 右侧主要内容区：
    - 顶部工具栏：包含“上传照片”按钮、当前分类名称。
    - 中央 `PhotoGrid`：以网格形式展示当前分类下的照片，支持点击查看大图或详情。
  - 上传照片时弹出 `UploadModal` 组件，选择文件和目标分类。
- **分类管理页**：独立的分类列表，支持添加新分类、重命名、删除分类（删除前需确认分类下照片的处理逻辑，建议可选移至默认分类或禁止删除非空分类，文档按先清空照片再删除设计，可交由后端校验）。

## 5. Pinia 状态管理设计

### 5.1 userStore（用户模块）

```javascript
state: () => ({
  token: localStorage.getItem('token') || '',
  userInfo: null
}),
getters: {
  isAuthenticated: (state) => !!state.token
},
actions: {
  async login(credentials) { ... },   // 存储 token 及用户信息
  logout() { ... }                    // 清除 token 及重置其他 store
}
```

### 5.2 categoryStore（分类模块）

```javascript
state: () => ({
  categories: [],       // 分类列表 [{ id, name, photoCount }]
  currentCategoryId: null
}),
getters: {
  currentCategory: (state) => state.categories.find(c => c.id === state.currentCategoryId)
},
actions: {
  fetchCategories() { ... },
  addCategory(name) { ... },
  updateCategory(id, name) { ... },
  deleteCategory(id) { ... },
  setCurrentCategory(id) { ... }
}
```

### 5.3 photoStore（照片模块）

```javascript
state: () => ({
  photos: [],                 // 当前分类下的照片列表
  loading: false,
  uploadProgress: 0
}),
actions: {
  fetchPhotos(categoryId) { ... },
  uploadPhoto(formData) { ... },   // formData 包含 file, categoryId
  deletePhoto(photoId) { ... },
  clearPhotos() { ... }
}
```

> 注意：切换分类时，调用 `photoStore.fetchPhotos(categoryId)` 刷新照片列表。

## 6. 功能模块详细设计

### 6.1 用户认证模块

- **功能点**：
  - 登录：提交用户名、密码，获取 token。
  - 登出：清除本地 token 及 store 状态，跳转登录页。
  - 路由守卫：未登录用户访问需认证页面时强制跳转 `/login`。
- **API 约定**：
  - `POST /api/auth/login`  
    Request: `{ username, password }`  
    Response: `{ code:200, data:{ token, user: { id, name } } }`
- **实现细节**：
  - 在 Axios 请求拦截器中自动添加 `Authorization: Bearer ${token}`。
  - 响应拦截器处理 401 状态码，自动登出。

### 6.2 分类管理模块

- **功能点**：
  - 展示用户所有的分类列表（侧边栏及分类管理页）。
  - 新增分类：弹窗输入名称，调用创建接口。
  - 编辑分类：修改分类名称。
  - 删除分类：调用删除接口，成功后刷新分类列表；若删除的分类为当前选中的分类，自动切换到默认分类或第一个分类。
- **API 约定**：
  - `GET /api/categories` → 返回分类数组
  - `POST /api/categories` → `{ name }`
  - `PUT /api/categories/:id` → `{ name }`
  - `DELETE /api/categories/:id`
- **业务规则**：
  - 每个用户至少存在一个默认分类（如“未分类”），不可删除默认分类。
  - 删除分类时，后端应将其下所有照片移动到默认分类，或返回错误由前端确认。

### 6.3 照片管理模块

#### 6.3.1 照片上传

- **功能点**：
  - 点击“上传照片”按钮，弹出模态框，选择文件（支持 jpg/png/gif，限制单文件≤10MB，可选多文件）。
  - 选择目标分类（下拉选择已有的分类）。
  - 显示上传进度，上传完成后刷新当前分类的照片列表。
- **API 约定**：
  - `POST /api/photos` （Content-Type: multipart/form-data）  
    FormData 字段：`file`（文件）、`categoryId`
  - 响应返回照片对象 `{ id, url, name, size, createdAt, categoryId }`

#### 6.3.2 照片展示与删除

- **功能点**：
  - 照片网格：使用 `PhotoGrid` 组件展示照片缩略图（后端提供缩略图 URL 或直接原图压缩显示）。
  - 每张照片支持“删除”按钮（需二次确认），删除后刷新列表。
  - 支持点击照片打开大图预览（可使用轻量级 lightbox 组件）。
- **API 约定**：
  - `GET /api/photos?categoryId=xxx` → 照片数组
  - `DELETE /api/photos/:id`

#### 6.3.3 照片分类移动（扩展功能，可选）

- 在照片详情或右键菜单中支持“移动至分类”，重新分配照片所属分类。若本期不做，可在文档中标注为 V1.1 计划。

### 6.4 其他辅助模块

- **路由守卫**：在 `router.beforeEach` 中检查 `userStore.isAuthenticated`，无 token 且目标页需要登录则重定向到 `/login`。
- **全局 loading**：通过 Pinia 或组件库的 loading 服务在请求时显示加载动画。

## 7. API 接口定义（完整列表）

所有接口均以 `/api` 为前缀，响应格式统一为：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 方法   | 端点                        | 描述                     | 权限        |
|--------|-----------------------------|--------------------------|-------------|
| POST   | `/auth/login`               | 用户登录                 | 公开        |
| GET    | `/categories`               | 获取用户所有分类         | 需登录      |
| POST   | `/categories`               | 新建分类                 | 需登录      |
| PUT    | `/categories/:id`           | 更新分类                 | 需登录      |
| DELETE | `/categories/:id`           | 删除分类                 | 需登录      |
| GET    | `/photos`                   | 获取某分类下的照片列表   | 需登录      |
| POST   | `/photos`                   | 上传照片                 | 需登录      |
| DELETE | `/photos/:id`               | 删除照片                 | 需登录      |

## 8. 组件拆分与复用说明

| 组件名               | 职责                                                         | 使用的 Store          |
|----------------------|--------------------------------------------------------------|-----------------------|
| `AppLayout.vue`      | 整体布局容器，包含侧边栏和主内容区域（用于 Dashboard 视图）   | -                     |
| `CategorySidebar.vue`| 展示分类列表，高亮当前选中分类，支持快速切换                 | categoryStore         |
| `PhotoGrid.vue`      | 接收 photos 数组，渲染网格，处理删除、点击预览等交互          | photoStore (调用删除) |
| `UploadModal.vue`    | 上传表单（文件选择、分类下拉），触发上传动作                 | photoStore, categoryStore |

## 9. 开发与构建流程

### 9.1 环境要求

- Node.js ≥ 16
- npm / pnpm / yarn

### 9.2 开发步骤

1. 克隆项目并安装依赖：
   ```bash
   npm install
   ```
2. 配置环境变量：创建 `.env.development` 文件，设置 `VITE_API_BASE_URL`（后端 API 地址）。
3. 启动开发服务器：
   ```bash
   npm run dev
   ```
4. 构建生产包：
   ```bash
   npm run build
   ```

### 9.3 代码规范

- 使用 ESLint + Prettier 统一代码风格，提交前自动格式化。
- 组件命名采用 PascalCase，组合式 API 中逻辑复用可使用 composables（如 `usePhotoUpload`）。

## 10. 注意事项与未来扩展

### 10.1 注意事项

- 上传照片需注意文件大小和类型的前端校验，减轻服务器压力。
- 删除分类时，应设计用户确认流程，并明确告知照片的迁移规则（建议默认移到“未分类”）。
- 由于 Token 存储在 localStorage，需防范 XSS 攻击，但本项目为基础版本，暂不做严格安全增强。
- 分页处理：当前设计未包含照片分页，如果照片数量较大，后端可增加 `page` 和 `limit` 参数，前端做滚动加载或分页组件。

### 10.2 未来可扩展功能（V2.0 规划）

- 照片批量上传与批量删除。
- 照片多分类标签支持（当前一个照片仅属于一个分类）。
- 照片搜索（按分类、上传日期、文件名）。
- 照片分享（生成公开链接）。
- 用户注册及个人资料管理。

## 11. 附录：Mock 数据参考（前端开发阶段）

若后端尚未就绪，可在项目中使用 `vite-plugin-mock` 或手动模拟 API 返回。示例 Mock 数据结构：

- 分类数据示例：
```json
[
  { "id": 1, "name": "旅行", "photoCount": 12 },
  { "id": 2, "name": "宠物", "photoCount": 5 },
  { "id": 3, "name": "未分类", "photoCount": 3 }
]
```

- 照片数据示例：
```json
[
  {
    "id": 101,
    "url": "https://picsum.photos/id/1015/300/200",
    "name": "mountain.jpg",
    "createdAt": "2025-03-01T10:00:00Z",
    "categoryId": 1
  }
]
```

---


## 12. 后端开发指南（Node.js + SQLite）

为配合前端相册应用的开发，后端采用 Node.js + Express 框架，使用 SQLite 作为轻量级数据库，便于本地测试与快速迭代。该方案无需额外安装数据库服务，所有数据存储于单文件。

### 12.1 后端技术栈

| 类别         | 技术选型                                    |
|--------------|---------------------------------------------|
| 运行时       | Node.js (≥18)                               |
| 框架         | Express                                     |
| 数据库       | SQLite3 + better-sqlite3（或 sqlite3 异步） |
| 文件存储     | 本地磁盘（`uploads/` 目录）                 |
| 跨域处理     | cors                                        |
| 身份认证     | jsonwebtoken (JWT) + bcryptjs               |
| 文件上传     | multer                                      |
| 辅助工具     | dotenv, nodemon（开发）                     |

### 12.2 后端目录结构示例

```
backend/
├── database/
│   ├── db.js                 # SQLite 连接及初始化表结构
│   └── schema.sql            # 建表语句
├── middleware/
│   ├── auth.js               # JWT 验证中间件
│   └── upload.js             # multer 配置文件上传
├── routes/
│   ├── auth.js               # 登录接口
│   ├── categories.js         # 分类管理接口
│   └── photos.js             # 照片上传、列表、删除接口
├── controllers/              # 可选：拆分业务逻辑
├── utils/                    # 工具函数（如 token 生成）
├── uploads/                  # 存储上传的图片文件（自动创建）
├── .env                      # 环境变量（端口、JWT密钥等）
├── server.js                 # 应用入口
└── package.json
```

### 12.3 数据库设计

#### 用户表 (users)

| 字段       | 类型        | 约束        | 说明               |
|------------|-------------|-------------|--------------------|
| id         | INTEGER     | PRIMARY KEY | 自增主键           |
| username   | TEXT        | UNIQUE NOT NULL | 用户名         |
| password   | TEXT        | NOT NULL    | bcrypt 哈希后的密码 |
| created_at | DATETIME    | DEFAULT CURRENT_TIMESTAMP | |

#### 分类表 (categories)

| 字段       | 类型        | 约束        | 说明               |
|------------|-------------|-------------|--------------------|
| id         | INTEGER     | PRIMARY KEY | 自增主键           |
| name       | TEXT        | NOT NULL    | 分类名称           |
| user_id    | INTEGER     | FOREIGN KEY | 所属用户           |
| is_default | INTEGER     | DEFAULT 0   | 是否为默认分类（1=是） |
| created_at | DATETIME    |             |                    |

> 每个用户至少有一个默认分类（如“未分类”），不可删除。

#### 照片表 (photos)

| 字段          | 类型        | 约束        | 说明                     |
|---------------|-------------|-------------|--------------------------|
| id            | INTEGER     | PRIMARY KEY | 自增主键                 |
| name          | TEXT        | NOT NULL    | 原始文件名               |
| url           | TEXT        | NOT NULL    | 存储路径或访问URL        |
| size          | INTEGER     |             | 文件大小（字节）         |
| category_id   | INTEGER     | FOREIGN KEY | 所属分类                 |
| user_id       | INTEGER     | FOREIGN KEY | 上传用户（便于权限隔离） |
| created_at    | DATETIME    |             |                          |

### 12.4 初始化数据库脚本（schema.sql 示例）

```sql
-- 用户表
CREATE TABLE IF NOT EXISTS users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 分类表
CREATE TABLE IF NOT EXISTS categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  user_id INTEGER NOT NULL,
  is_default INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- 照片表
CREATE TABLE IF NOT EXISTS photos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  url TEXT NOT NULL,
  size INTEGER,
  category_id INTEGER NOT NULL,
  user_id INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

> 在服务启动时，自动执行建表语句，并检查是否存在测试用户（如 `admin / 123456`）及对应的默认分类。

### 12.5 API 实现要点

#### 12.5.1 登录接口（POST /api/auth/login）

- 接收 `username`、`password`。
- 查询用户，使用 `bcrypt.compare()` 验证密码。
- 验证通过后生成 JWT token（有效期建议 7 天），返回 token 及用户信息（id, username）。

#### 12.5.2 分类接口（需 JWT 验证）

- **GET /api/categories**  
  查询 `categories` 表，`WHERE user_id = 当前用户 id`，同时可关联查询每个分类下的照片数量（子查询或聚合）。
- **POST /api/categories**  
  创建新分类，需校验名称不能重复（同用户下）。
- **PUT /api/categories/:id**  
  更新分类名称，校验权限（分类必须属于当前用户）。
- **DELETE /api/categories/:id**  
  删除分类前需检查是否为默认分类（`is_default === 1`），若是则返回错误。删除后，将该分类下的所有照片的 `category_id` 更新为默认分类的 id（在事务中执行）。

#### 12.5.3 照片接口

- **GET /api/photos**  
  接收 query 参数 `categoryId`（可选），返回当前用户且有权限访问的照片列表。若未提供 categoryId，返回所有照片（建议按创建时间倒序）。
- **POST /api/photos**（multipart/form-data）  
  使用 multer 处理文件上传，存储到 `uploads/` 目录，生成唯一文件名（如时间戳+随机数），保存记录到 `photos` 表。需校验文件类型（图片）和大小（如 ≤10MB）。
- **DELETE /api/photos/:id**  
  删除照片记录的同时，删除磁盘上的实际文件。需校验照片属于当前用户。

### 12.6 文件存储策略

- 上传目录：`backend/uploads/`。
- 访问静态资源：通过 Express `express.static('uploads')` 提供，前端获取照片 URL 为 `http://localhost:3000/uploads/filename.jpg`。
- 为提升性能，可在返回照片列表时同步返回缩略图（本版不要求后端生成缩略图，由前端 CSS 控制显示比例）。

### 12.7 环境变量（.env 示例）

```env
PORT=3000
JWT_SECRET=your_super_secret_key_change_me
DB_PATH=./database/app.db
```

### 12.8 启动与开发流程

1. 进入 `backend` 目录，安装依赖：
   ```bash
   npm install express sqlite3 better-sqlite3 jsonwebtoken bcryptjs multer cors dotenv
   ```
   （开发依赖：`nodemon`）
2. 创建 `.env` 文件并配置。
3. 运行初始化脚本（在 `server.js` 中调用建表及初始数据生成）。
4. 启动服务：
   ```bash
   node server.js
   # 或使用 nodemon 热重启
   npx nodemon server.js
   ```
5. 后端服务默认运行在 `http://localhost:3000`，前端通过 `VITE_API_BASE_URL=http://localhost:3000/api` 进行联调。

### 12.9 关键代码片段示例

**server.js 核心结构：**

```javascript
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const path = require('path');
const { initDB } = require('./database/db');

const app = express();
app.use(cors());
app.use(express.json());
app.use('/uploads', express.static(path.join(__dirname, 'uploads')));

// 初始化数据库
initDB();

// 路由挂载
app.use('/api/auth', require('./routes/auth'));
app.use('/api/categories', require('./routes/categories'));
app.use('/api/photos', require('./routes/photos'));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
```

**auth 中间件示例：**

```javascript
const jwt = require('jsonwebtoken');
module.exports = (req, res, next) => {
  const authHeader = req.headers.authorization;
  if (!authHeader) return res.status(401).json({ code:401, message:'未提供 token' });
  const token = authHeader.split(' ')[1];
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.userId = decoded.userId;
    next();
  } catch (err) {
    res.status(401).json({ code:401, message:'token 无效' });
  }
};
```

### 12.10 测试数据初始化

建议在 `initDB()` 中检查用户表是否为空，若为空则创建默认测试用户（用户名 `test`，密码 `test123`，使用 bcrypt 加密），并为该用户自动创建默认分类（“未分类”）。这样前端可直接使用该账号登录测试。

### 12.11 前后端联调注意事项

- 前端请求需携带 `Authorization: Bearer <token>`。
- 开发环境下前端 Vite 代理可配置避免跨域：在 `vite.config.js` 中设置 proxy 指向 `http://localhost:3000`。
- 上传文件时，前端需使用 `FormData` 并设置 `headers: { 'Content-Type': 'multipart/form-data' }`（Axios 会自动处理）。

---

> 上述后端设计仅为测试用途，后续可平滑迁移到 MySQL/PostgreSQL，仅需修改数据库连接部分。请将后端代码单独存放于项目根目录下的 `backend` 文件夹，与前端代码保持独立。